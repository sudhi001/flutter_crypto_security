name: flutter_crypto_security
description: "RSA + AES-GCM encryption, signing and hybrid envelopes, interoperable with the Go crypto_utils package and Rust crate."
version: 0.1.0
publish_to: none
homepage: "https://github.com/sudhi001/flutter_crypto_security"  # Add the URL of your package homepage, like a GitHub repo.

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  pointycastle: ^4.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0


flutter:

